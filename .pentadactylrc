" google als standardsuche
set defsearch=g 

" beim laden einer seite nicht in den insert modus wechseln
" set focuscontent 
" bei seiten wie google, kommt man mit 'gi' direkt ins inputfeld

" set ignorecase 
" case nicht ignorieren, wenn uppercase letter dabei sind
" set smartcase 

" search while typing
" set incsearch 

"link unter cursor in statusline anzeigen
set showstatuslinks=status

"set autocomplete=

set shell=/bin/zsh
colorscheme corrupt

" defines which commands open in a new tab
" (all, addons, downloads, extoptions, help, javascript, prefs)
set newtab=all

" defines when the tabbar is visible
" (always, multitab, never)
set showtabline=multitab

set go+=n

" vim:ft=vim
